# 💗 推しログ（OshiLog）

推し活をぜんぶ記録できる、推し活サポートアプリです。
アジャイル開発課題の「動くたたき台」から発展させ、**紙の推し活手帳**をコンセプトにした
本番仕様（SNS・リアルタイム機能つき）にアップグレードしました。

## 🌐 デプロイURL（本番環境）

**https://web-production-bd33b.up.railway.app**

- ID・パスワードで登録／ログインできます（メールアドレス不要）。
- お試し用アカウント：`demo` / パスワード `demo`
- **管理者アカウント：`admin` / パスワード `oshilog-admin`**（イベントの作成・編集・削除ができます）

## ✨ 機能一覧

### 基本機能
| 機能 | 内容 |
|---|---|
| 推し登録・管理 | ジャンルブロック→タップでポラロイド風タイルが展開。同名の推しは共有マスターに集約し「◯人が登録中」を表示 |
| カレンダー | iPhoneカレンダー準拠の縦スクロール連続月ビュー。日付タップで選択、ダブルタップで1日詳細、予定検索 |
| 参戦記録・家計簿 | 日付・内容・金額・メモを記録。月別合計、推し別の円グラフ・月別推移の棒グラフ |
| グッズコレクション | 画像付きで登録、カテゴリで絞り込み |
| つぶやき | 公開範囲を選んで投稿。タイムラインにリアルタイム反映 |

### 追加機能（本番仕様）
| 機能 | 内容 |
|---|---|
| 認証・プロフィール | ID＋パスワード（**scryptでハッシュ化**して保存）。表示名・アイコン・自己紹介を編集 |
| 推し友（フレンド） | 相互承認制。同じ推しの人をランダムでおすすめ（共通の推し名を表示） |
| リアルタイムトーク | Socket.ioによるDM・イベントグループチャット。推し友／参加者だけがアクセス可能 |
| つぶやき公開範囲 | プライベート／全体／同じ推し／同じイベント参加者の4種。**サーバー側で閲覧可否を判定** |
| 共通イベント | 管理者が作成。参加すると個人カレンダーに自動追加＋参加者専用チャットに自動参加 |
| 予定共有 | 予定ごとに共有ON/OFF。共有予定は推し友のカレンダーに色分け表示 |
| 管理者機能 | `/admin` でイベントのCRUD・参加者数確認（**権限はサーバー側で判定**、一般ユーザーは不可） |
| 通知（Web Push） | PWA化＋VAPID方式のプッシュ通知（メッセージ・申請・イベントリマインド） |

## 🛠 技術スタック

- **フロントエンド**: React 19 + Vite 7 + TailwindCSS 4 + recharts + React Router + socket.io-client
- **バックエンド**: Node.js + Express + Socket.io + web-push
- **DB**: PostgreSQL（Railway）
- **認証**: ID/パスワード（scryptハッシュ＋HMAC署名トークン）
- **デプロイ**: Railway（Dockerfile。Expressがフロントのビルド成果物を配信する1サービス＋PostgreSQL）
- **PWA**: manifest.json + Service Worker（ホーム画面追加・Web Push対応）

## 🎨 デザイン

紙の推し活手帳をコンセプトに、ベージュ（`#E8DFCE`）の背景に罫線、
アクセントはワインレッド（`#8B3A4A`）。推しタイルやグッズはポラロイド風、
カレンダーの選択日はスタンプの丸囲み風。蛍光色・ネオンカラーは使用していません。

## 💾 主なDBテーブル

`users`(is_admin・is_public・auto_reject_requests・notify_*等) / `oshi_master`(共有マスター・official_url・goods_url) / `oshi`(個人の推し) /
`schedules`(event_id・start_time・end_time・url・reminder_offset_minutes) / `schedule_shares`(予定の共有先) /
`notifications`(通知センターの履歴) /
`records`(event_id) / `goods` / `posts`(visibility・event_id) / `diary_entries`(日記・visibility) /
`oshi_images`(着せ替え審査) / `user_oshi_display_image`(推しの表示画像をユーザーごとに選択) /
`friendships` / `chat_rooms` / `chat_room_members` /
`chat_messages`(attachment_*) / `chat_message_reads`(既読) / `album_photos`(共有アルバム) /
`events` / `event_participants`(savings_goal) / `savings_transactions`(貯金の入出金) /
`blocks`(ブロック) / `pinned_chats`(トークのピン止め) / `push_subscriptions`

テーブルはサーバー起動時に自動作成・マイグレーションされます（`CREATE TABLE IF NOT EXISTS` ＋ `ALTER ... ADD COLUMN IF NOT EXISTS`）。

## 🚀 ローカルでの起動方法

Node.js 20以上と、接続できるPostgreSQLが必要です。

```powershell
# 依存インストール
cd backend; npm install
cd ../frontend; npm install

# バックエンド起動（別ターミナル）
cd backend
$env:DATABASE_URL = "postgresql://ユーザー:パスワード@ホスト:5432/DB名"
$env:TOKEN_SECRET = "任意の秘密鍵"
# 通知を試す場合は VAPID_PUBLIC_KEY / VAPID_PRIVATE_KEY も設定（web-push generate-vapid-keys で生成）
node server.js

# フロントエンド起動（開発モード。/api と /socket.io は localhost:3000 へプロキシ）
cd frontend; npm run dev
```

ブラウザで http://localhost:5173 を開く。

## ☁️ Railwayへのデプロイ

デプロイ設定は `railway.json` にコード化してある（Dockerfileビルド＋`/api/health` ヘルスチェック）。

### 方法A：GitHub連携で自動デプロイ（推奨）
Railwayダッシュボードで `web` サービスを本リポジトリの `main` ブランチに接続すると、
`main` へpushするたびに自動でビルド＆デプロイされる。
- Railway → oshilog → `web` → Settings → Source → Connect Repo で `tarutoon7391/OshiLog` の `main` を指定

### 方法B：CLIから手動デプロイ
```powershell
railway up --ci -s web   # リポジトリ直下で実行（Dockerfileでビルド）
```

環境変数（`web` サービス）：
- `DATABASE_URL = ${{Postgres.DATABASE_URL}}`（参照変数）
- `TOKEN_SECRET`（トークン署名用）
- `VAPID_PUBLIC_KEY` / `VAPID_PRIVATE_KEY` / `VAPID_SUBJECT`（Web Push用）
- `ANTHROPIC_API_KEY`（貯金サポートAI用・任意。未設定でもルールベースで動作）
- `GOOGLE_MAPS_API_KEY`（会場の地図・ルート表示用・任意。未設定でも地図はリンク表示にフォールバック）

#### Google Maps APIキーの取得・設定手順
1. [Google Cloud Console](https://console.cloud.google.com/) でプロジェクトを作成（または選択）
2. 「APIとサービス」→「ライブラリ」で以下を有効化する
   - **Maps Embed API**（イベント画面の地図埋め込み用）
   - **Routes API**（最寄り駅までの公共交通ルート用）
3. 「APIとサービス」→「認証情報」→「認証情報を作成」→「APIキー」でキーを発行
4. （推奨）発行したキーに「アプリケーションの制限（HTTPリファラー）」「APIの制限（上記2つのAPIのみ）」を設定
5. Railway → `web` サービス → Variables に `GOOGLE_MAPS_API_KEY = <発行したキー>` を追加
   （ローカルでは `$env:GOOGLE_MAPS_API_KEY = "<キー>"` を設定してから `node server.js`）
   - キーはサーバー側環境変数としてのみ保持し、フロントのソースには直書きしない
   - 未設定でもアプリは動作する（地図はGoogleマップを開くリンク表示、ルートは非表示になる）

## 🔔 通知（Web Push）の使い方

1. プロフィール画面の「通知をオンにする」をタップ→ブラウザの許可ダイアログで許可
2. 以降、推し友のメッセージ・申請、参加イベントのリマインドが通知されます
3. **iPhone/iPadは、Safariの共有→「ホーム画面に追加」をしてから起動しないと通知が機能しません**
4. 通知を拒否しても、アプリの他の機能は問題なく使えます（通知は必須ではありません）

## 📝 今後の拡張予定（要件整理用メモ）

- [x] メッセージの既読表示・未読バッジ（第2弾で対応）
- [ ] 推しマスターの表記ゆれ吸収（別名・英語名）
- [ ] 画像はストレージ保存に変更（現在はBase64でDB保存・2MB上限）
- [ ] イベントのカテゴリ・ジャンル別フィルタ
- [ ] 通知の種類ごとのON/OFF設定

## 📜 更新履歴

### 2026-07-13（バグ修正：クイックログインでパスワードが正しくても失敗することがある問題）
- アカウントを選んでログインする画面で、初期表示のまま（または「保存済みアカウントから選ぶ」で
  戻った直後に）パスワードを入力すると、送信されるユーザーIDが空になりログインに失敗していた。
  クイックログイン中は選択中アカウントのIDを必ず送信するように修正

### 2026-07-13（第16弾：家計簿グラフ／ID検索／ブロック仕様修正／推し友削除／カレンダー詳細化／木の実の演出）
- **家計簿グラフの強化**：月別支出の棒グラフに「直近6ヶ月／全期間」の切り替えタブを追加
  （`/api/stats/summary` に全期間の月別集計 `monthlyAll` を追加）。
  月合計は判子風の枠＋カウントアップ表示、見出しはマステ風、罫線風の区切り線など手帳らしい装飾も追加
- **推し友のID検索**：「さがす」タブにログインID検索を新設（`GET /api/users/search`）。
  その場で申請ボタン／推し友・申請済みの状態表示／見つからない場合の案内を表示。
  ブロック関係にある相手はサーバー側で検索結果から除外
- **ブロック仕様の変更（LINE方式・判定はすべてサーバー側）**
  - ブロックしても**推し友関係は維持**される（従来の自動解消を廃止）
  - ブロック中に相手が送ったメッセージは、**ブロックした側にだけ**表示されない・通知も届かない
    （送信時点で `chat_messages.hidden_for_user_ids` に記録。メッセージ取得・トーク一覧の
    プレビュー・未読数・既読処理・リアルタイム配信・プッシュ通知のすべてでサーバー側フィルタ）
  - ブロックされた側は通常どおり送信でき、エラーにならない（既読が付かないだけ）。
    プロフィール表示・申請時のレスポンスからもブロックされていることが分からないようにした
  - ブロック解除後は通常どおり届く（ブロック中に送られた分は非表示のまま＝LINEと同じ）
  - つぶやきの非表示・おすすめからの除外は従来どおり
- **推し友の削除（アンフレンド）**：推し友一覧に「解除」ボタンを追加（`DELETE /api/friends/:id`）。
  ブロックとは別で関係だけを解消し、トーク履歴とメッセージ送受信はそのまま。再申請も可能。
  再承認時は既存のDMルームを再利用してトークが二重にできないようにした
- **カレンダー日別ビューの詳細化**：0時〜24時の時間軸をすべて罫線付きで表示（開くと最初の
  予定の時間帯まで自動スクロール）。予定の開始・終了時刻は**15分単位の選択式**に変更
  （既存の15分刻み以外のデータも消えないよう選択肢に含める）
- **ホームの木の演出**：木（実以外の部分）をタップすると木が揺れて実が落ち、画面下部に
  2段で整列する。落ちた実をタップすると従来どおり拡大→モーダル表示。もう一度木をタップ
  すると実が木に戻る（transform/left/topのみの軽量実装。`prefers-reduced-motion`で無効化）

### 2026-07-13（第10弾改良版：モーションの上乗せ＋差し色パープルの導入）
- **マイクロインタラクションの追加**（すべてtransform/opacity中心の軽量CSS。`prefers-reduced-motion`で無効化）
  - 通知ベル：新着が届いた瞬間に「リンッ」と揺れる。未読バッジは件数が変わるたびにぽんっと弾む
  - ボトムナビ：選んだタブのアイコンが小さく跳ね、ゴールドのバーが「マステを貼る」ように左から伸びる
  - 進捗バー：初回表示時も左からぐっと伸び、伸びきった後に**箔押しの光沢**が一度だけ走る
  - AI相談FAB：判子をトンと押すように登場
  - モーダル：背景の暗幕がふわっとフェードイン＋見出し下に箔押しゴールドの飾りライン
  - 参戦記録の判子演出を強化：判子のまわりを3色のハート（💗💜💛）が時間差で舞い上がる
  - ログイン画面：カードがふわっと現れ、ロゴのハートがゆっくり鼓動。保存済みアカウントは時間差表示
  - 金額・件数の**カウントアップ演出**（JS）：ホームの「今月の推し活費」「推している人」「あと◯日」
  - 引っ張って更新：リングが満タン（離せば更新）になると差し色パープルに変化
  - 空っぽ表示のアイコンがふわふわ揺れる。マステ見出しはホバーでまっすぐ貼り直される
  - 記録/集計タブ・ログイン切替などの小ボタンにも押し込み＋色のトランジションを追加
- **配色調整（差し色パープルの導入。蛍光色・ネオンは不使用、ベース配色は変更なし）**
  - Twitchブランドカラー付近のパープル（`--color-pop: #9146ff`）を**少量のワンポイント専用**として追加
  - 使用箇所：ヘッダーの未読バッジ（ゴールド→パープルに変更）・通知センターの未読ドット・
    主ボタンとFABをタップした瞬間のグロウ・引っ張って更新の満タン色 の4箇所のみ
  - ベースの「紙×ワイン×箔押しゴールド」はそのまま。パープルはあくまでポップな差し色

### 2026-07-07（第15弾：通知ベル＋通知センター＋カテゴリ別オン/オフ）
- **通知ベルマーク**：ヘッダーのプロフィールアイコンの隣に🔔を常時表示。未読件数をゴールドのバッジで表示
  （画面遷移時に取得＋新着はSocket `notification:new` で即時反映）
- **通知センター（/notifications）**：ベルをタップで通知履歴の一覧へ。`notifications` テーブルを新設し、
  新しい順に既読・未読の両方を表示（未読は太字＋ワインの縁取り＋●で区別）。
  通知をタップすると**既読にした上で** `link_url` の遷移先へ移動。「すべて既読にする」ボタンあり。
  既読化はサーバー側で本人確認
- **全Push通知を通知センターにも記録**：通知送信を `notify.js` に一元化し、
  推し友申請／推し友成立／トーク（DM）／グループトーク／イベントリマインド／予定の個別リマインド／
  着せ替え審査結果／推しの新規イベント追加の全てが Push送信と同時に履歴へ記録される。
  チャット通知のリンク先を `/friends` → `/chat/{roomId}`（該当トーク）に改善
- **通知カテゴリ別のオン/オフ**：プロフィール設定に4カテゴリのトグルを追加
  （`users.notify_friend_request / notify_chat_dm / notify_chat_group / notify_event`、デフォルトすべてオン）。
  **オフのカテゴリは Push送信・通知センターへの記録の両方をスキップ**（履歴にも残らない）。
  着せ替え審査結果・推しの新規イベント追加・予定の個別リマインド（本人が予定ごとに設定するもの）は
  オン/オフの対象外で常に有効

### 2026-07-07（第14弾：カテゴリ内の人気順表示＋イベント重要度の色分け）
- **同じカテゴリー内のおすすめ表示**：推し画面の各ジャンル内・検索結果を**登録人数の多い順（人気順）**で表示。
  既存の登録人数カウント（`registered_count`）をそのまま並び替えに使用（新しいランキングデータは不要。
  APIは従来から人気順で返しており、画面側でも明示的にソートして順序を保証）
- **イベント通知の多段階化について**：ご要望の「2週間前・1週間前・3日前・2日前・前日」の多段階通知は、
  **第8弾で実装済み**（実際には当日を含む6段階：14/7/3/2/1/0日前）。参加登録している全ユーザーへ
  6時間ごとの定期チェックで送信し、`event_participants.last_reminded_days`（最後に通知した残り日数）で
  同じ段階の二重送信を防止している。今回の変更はなし（仕組みはそのまま有効）
- **イベントの重要度設定**：参加イベントごとに「ふつう／重要／最重要」を設定できる
  （`event_participants.importance`。**値の検証・本人確認はサーバー側**）。
  イベント一覧の参加中カードに重要度セレクタを設置（参加直後から変更可能）
- **重要度の色分け表示**（蛍光色・ネオンは不使用。既存アクセントの濃淡＋箔押しゴールドの縁）
  - 重要（⭐）：ワインレッドの強調（カレンダーのチップ・予定の縁取り・カードの左帯）
  - 最重要（🌟）：さらに濃いワイン＋ゴールドの縁取り
  - 反映箇所：カレンダー（月セルのチップ・1日詳細）／ホームの今日・直近の予定／イベント一覧／イベント履歴

### 2026-07-07（第13弾：ホーム画面を「木の果実」ビジュアルに再構成）
- **手描き落書き風の木**：手帳の余白に描いたようなラフな線画SVG（フリーハンド風の幹2本線・
  もこもこの樹冠・葉の走り書き・ハートの落書き）をホーム中央に配置。低彩度の紙になじむ色のみ使用
- **ホームの全要素を「果実」化**：いちばん近い予定（カウントダウン付き・ワイン色の大きな実）／
  今日・直近の予定（件数）／貯金状況（達成率%・金縁）／今月の推し活費／推している人／
  わたしの推し／家計簿／グッズ の8種を枝先に配置。貯金目標が無いときは貯金の実は実らない
- **果実タップ→その場で拡大→モーダル**：タップした果実の中心を起点に拡大しながらモーダルが開く
  （第7弾のカレンダーズーム遷移と同じ考え方。`transform`/`opacity`のみ・`prefers-reduced-motion`で無効化）。
  背景は暗転し、閉じると逆再生で果実へ戻る。**ページ遷移は発生しない**
- 各モーダルの中身は既存ホームの表示・データ取得ロジックをそのまま移植（APIの変更なし）
- 果実は風で揺れるようにゆっくり上下する（果実ごとに時間差）

### 2026-07-07（第12弾：クライアント聞き取りからの追加要望8件）
- **貯金状況をホームに表示**：貯金目標が設定されているイベントのうち最も開催が近いものを、
  ホーム上部に達成度バー付きで表示（残高＝入金合計−出金合計）。タップでそのイベントの
  入出金モーダルへ遷移（`/events?focus=ID&savings=1` で自動オープン）。目標つきイベントが無ければ非表示
- **推し画面に「わたしの推し」セクション**：推しタブ上部（イベント導線のすぐ下）に、
  ホームと同じ登録済み推しのアイコン一覧を表示。タップで各推しの詳細ページへ
- **推しの検索機能**：推し画面に検索バーを追加。ジャンルを問わず名前の部分一致で検索し、
  結果はポラロイドタイルで一覧表示（その場で登録も可能）。0件のときはその旨を表示
- **予定に関連URL**：予定の作成・編集にURL入力欄（任意）を追加（`schedules.url`）。
  チケットサイト・配信ページ等を保存でき、1日詳細でタップ可能なリンクとして表示。
  **http/https 以外のスキームはサーバー側で拒否**（javascript: 等の不正URL対策）
- **予定の個別リマインド**：予定ごとに通知タイミングを選択可能
  （なし／5分前／15分前／30分前／1時間前／3時間前／1日前。`schedules.reminder_offset_minutes`）。
  バックエンドが**5分おき**に予定をチェックし、「予定日時（日本時間・時刻未指定は0:00扱い）−オフセット」を
  過ぎたら既存のWeb Pushで通知。**送信済み印（`reminder_sent_at`）を先に原子的に付けてから送る**ことで
  二重送信を防止（日時やタイミングを変更すると印はリセットされ、改めて1回通知される）
- **推し友申請の自動拒否設定**：プロフィール設定にオン/オフを追加（`users.auto_reject_requests`）。
  オンのユーザーへの申請は保存・通知されず即座に拒否扱い（既存の拒否と同じ最終状態＝再申請可能）。
  申請者へのレスポンスは通常送信と同じにして、設定の有無を漏らさない。**判定はサーバー側**
- **ブロックユーザー一覧画面**：第9弾で実装済みのブロックリスト（マイページ →「🚫 ブロックリスト」。
  表示名・アイコン表示＋ブロック解除ボタン。解除はサーバー側で本人確認）を今回の要望どおり継続提供
- **ホームの予定表示ロジック変更**：今日の予定がある場合は「📌 今日の予定」として**全件**表示（上限なし）。
  それとは別に**今日より後**の直近予定を最大3件表示し、一番近い1件だけワインレッドのカウントダウンカードで強調

### 2026-07-06（第10弾：全体的なモーション追加＋配色の調整）
- **マイクロインタラクション**（すべてtransform/opacityのみの軽量CSS。`prefers-reduced-motion`で無効化）
  - ボタン全般：押すと軽く沈む（`press`）。枠線ボタンはホバーで薄く色づく
  - 一覧の登場演出：推しタイル・グッズ・つぶやき・イベント・参戦記録・マイページ・貯金目標が
    紙を1枚ずつ置くように時間差でふわっと表示（`stagger`）
  - 画面遷移：便箋を1枚重ねるようなフェード＋浮き上がり（`page-in`）
  - モーダル：下からせり上がる（`sheet-up`）
  - 通知トースト：退場時に上へ戻るアニメーションを追加
  - **参戦記録の判子演出**：記録登録時に「参戦記録!」の判子が画面中央にトンと押される
  - 進捗バー：伸びるアニメーション（0.7秒）
  - ポラロイドタイル：ホバーで浮き上がる影
- **配色調整**（蛍光色・ネオンは不使用。ベース背景は変更なし）
  - ワインレッドの彩度を少し強化：`#8b3a4a` → `#96324e`（テーマ「ワイン」も同様）
  - **箔押しゴールド**（`--color-gold: #b9962e`）をワンポイントで追加：
    セクション見出しの左端・選択中タブの上のマステ風バー・AI相談FABの内輪・進捗バーの先端グラデーション

### 2026-07-06（第9弾：ナビゲーションの整理＋AI相談ボタンの新設）
- **ボトムナビを4項目に整理**：ホーム／予定／推し／推し友。
  外した機能はすべて別の導線から辿れる（下記マイページ・推しタブ参照）
- **マイページの新設**：右上のユーザーアイコンをタップ→マイページへ。
  プロフィール設定・つぶやき・日記帳・貯金目標一覧＆AI相談・グッズ・参戦記録/家計簿・
  イベント履歴・アカウント公開設定・ブロックリスト・サイト案内AI・（管理者のみ）管理者画面 への導線を集約
- **推しタブのハブ化**：推しブラウズ上部に「🎪 イベント一覧」「🕘 イベント履歴」への導線を追加
- **AI相談FABの新設**：右下に判子風の丸ボタン（💬 AI相談）を常時表示。タップで**サイト案内AI**（旧・案内タブ）へ。
- **貯金サポート画面の新設**：マイページの「貯金目標一覧・AI相談」から。参加中イベントを選んで
  貯金サポートAIに相談＋貯金目標の一覧表示（入出金は従来どおりイベントページから）
- **ブロックリスト画面の新設**：ブロック中のユーザーの確認・解除（`GET /api/blocks` を追加。解除は既存API）
- 既存機能のロジックは変更なし（導線の整理のみ）

### 2026-07-06（第8弾：通知の追加2種類＋リマインドのカウントダウン化）
- **着せ替え画像の審査結果通知**：管理者が着せ替え画像を承認／却下した瞬間、投稿者本人へ
  「[推しの名前]の画像が承認されました／却下されました」をプッシュ通知。タップでその推しの詳細ページへ
  （同じ状態への再操作では送らない。投稿者不明の画像はスキップ）
- **推しの新規イベント追加通知**：管理者がイベントを作成した瞬間、そのアーティスト（`events.artist_id`）を
  推し登録している全ユーザーへ「[アーティスト名]の新しいイベント「[イベント名]」が追加されました」を通知。
  タップで該当イベントへ（`/events?focus=ID` で自動スクロール＋ハイライト）。`artist_id` 未設定のイベントは送らない
- **イベントリマインドのカウントダウン化**：従来の「前日・当日に1回」から、
  **2週間前・1週間前・3日前・2日前・前日・当日**の6段階に拡張（「⏰ あと1週間」「⏰ いよいよ明日！」など）。
  `event_participants.last_reminded_days` で同じ段階の二重送信を防止
- いずれも既存のWeb Push基盤（`web-push`＋`push_subscriptions`）をそのまま流用。
  通知許可のないユーザーには送信自体をスキップ（エラーにしない）

### 2026-07-06（第7弾：カレンダーのUI/UX改善）
- **日付タップの挙動変更**：ダブルタップ（素早い2回タップ）判定を廃止。
  未選択の日をタップ→選択（丸ハイライト）、**選択中の日をもう一度タップ→1日詳細へ**、という
  「選択中かどうか」の状態ベースの単純な操作に変更（iPhone標準カレンダー準拠）
- **年→月→日の3階層ナビゲーション**：12か月ミニチュアの**年ビュー**を新設。
  画面上部の戻るボタンはiPhoneカレンダーと同じ「シェブロン（＜）＋遷移元の階層名（例：2026年／7月）」構造
  （配色はiOS標準の赤ではなく既存アクセントのワインレッド `#8B3A4A` を使用）
- **ズームイン・モーフィング遷移**：年→月はタップした月を起点に拡大、月→日はタップした日付セルを起点に
  拡大して開き、戻るときは逆再生で縮小。`transform`／`opacity` のみのCSSアニメーションで軽量に実装し、
  `prefers-reduced-motion` 設定時は無効化

### 2026-07-06（通知バグ修正：iOSで通知が届かなくなる問題）
- **原因**：アプリ表示中はOS通知の代わりにアプリ内トーストを出す第1弾修正の副作用。iOSはバックグラウンドの
  PWAでも `visibilityState='visible'` のまま残るため「開いている」と誤判定し、さらにWebKitは通知を表示しない
  プッシュ受信が続くと**購読自体を自動失効**させるため、以後まったく届かなくなっていた
- **対策**：iOS（Apple購読）では常にOS通知を表示。その他ブラウザは「visibleかつフォーカス中」のときだけトースト。
  起動時に購読を自動再同期する自己修復（`resyncPush`）と、プロフィールに再設定リンクを追加。送信エラーをログに記録

### 2026-07-02（クライアント用ログイン＋サイト案内AIチャットボット）
- **クライアント用ログイン**：`is_client` ロールを新設し、クライアント（発注者・レビュー担当）アカウントをシード
  （ログインID: `client` / パスワード: `oshilog-client`）。ログイン時のみフッターに「💬 案内」タブが表示される。
- **クライアントメニュー**：中心に**「推しログについて何でも答えるサイト案内AI」**を用意。
  アプリの全機能を説明したナレッジをシステムプロンプトに持たせ、使い方・機能・仕様を日本語で回答する。
- **Claude API 流用・質問制限なし**：既存の Anthropic 連携（`ANTHROPIC_API_KEY`）をそのまま使用。
  貯金サポートAIと違い**質問回数の上限は設けない**。利用できるのはクライアント／管理者のみ（判定はサーバー側）。
  キー未設定時は案内メッセージにフォールバック。

### 2026-07-02（全国の著名な会場をシード投入）
- **会場マスターの初期データ**：全国の著名なライブ・コンサート会場（ドーム・アリーナ・ホール・展示場など約40件）を
  起動時シードに追加（同名があればスキップ＝冪等）。緯度経度は概算で、管理者が「🛠 管理 → 会場」から編集・追加可能。
- デモイベントの `location` 名が会場名と一致するものは、会場（`venue_id`）を自動で紐付け（地図表示をすぐ試せるように）。

### 2026-07-02（管理者の推し管理を拡充：新規追加・画像/名前登録）
- **推しの新規追加（管理者）**：これまで推しマスターは一般ユーザーが登録したときだけ作られていたが、
  管理メニューの「⭐ 推し」タブから**管理者が新しい推し（アーティスト）を直接追加**できるようにした
  （まだ誰も登録していない推しでも、先に管理者が登録して情報を整備できる）。`POST /api/admin/oshi-master` を追加
- **名前・ジャンルの編集（管理者）**：既存の推しの**名前・ジャンル**も管理者が編集できるようにした（従来はジャンルとURLのみ）。
  名前変更時は重複チェックあり
- **代表画像の直接登録（管理者）**：ユーザーからの着せ替え申請を待たずに、**管理者が推しの代表画像を直接アップロード／差し替え**できるようにした
  （画像未指定の編集では既存画像を維持）。従来どおり着せ替え画像の承認・却下も可能
- いずれも**管理者判定はサーバー側**で実施（一般ユーザーの追加・編集は403で拒否）

### 2026-07-02（第4弾：イベント会場の地図表示・アクセス情報）
- **会場（venue）マスターの新設**：イベント会場を独立マスターとして管理する `venues` テーブルを追加
  （名称・住所・緯度・経度・最寄り駅・運賃メモ）。`events` に `venue_id`（任意の外部キー）を追加。
  **会場の登録・編集・削除は管理者のみ**（管理メニューに「📍 会場」タブを追加。判定はサーバー側で実施）。
  最寄り駅・運賃メモ（`fare_note`）は任意項目で、未入力でも会場登録・イベント作成が可能
- **地図表示**：イベント画面で、紐づく会場の位置を Google Maps Embed API で表示（緯度経度にマーカー）。
  **APIキーはサーバーの環境変数 `GOOGLE_MAPS_API_KEY` のみで管理**し、フロントのソースには直書きしない
  （実行時に `/api/maps/config` 経由で受け渡す）。キー未設定時はGoogleマップを開くリンクにフォールバック
- **最寄り駅までのルート表示**：Google Routes API（`travelMode: TRANSIT`）を**サーバー側**で呼び出し、
  会場→最寄り駅の公共交通ルート（所要時間・距離）を表示。キー未設定・最寄り駅未入力なら非表示
- **運賃メモの表示**：運賃はGoogle側の日本の鉄道データが不安定なため**自動取得しない**。
  目安が必要な場合は、管理者が `fare_note` に自由記述した内容をそのまま表示（未入力なら非表示）

### 2026-07-02（プロフィール遷移導線の修正／つぶやき関連バグ・仕様変更）
- **プロフィール遷移導線の統一**：ユーザーアイコン用の共通コンポーネント `UserChip` を新設し、
  タップで `/users/:id` へ遷移する挙動を内蔵。DMトーク一覧・イベントチャットの参加者一覧・
  推し友一覧・タイムライン（つぶやき）・申請一覧・おすすめの各所に適用し、遷移導線の付け忘れを防止
  （イベントチャットのヘッダーから参加者一覧モーダルを開けるよう `/chat/rooms/:id/members` を追加。メンバー限定）
- **同担限定つぶやきの公開範囲バグ修正**：「同じ推しの人にのみ公開」の投稿が、その推しを登録していない
  ユーザーにも見えてしまう不具合を修正。タイムラインのGET取得（`visibilityWhere`）と
  Socket.ioのリアルタイム配信（`oshi_{oshi_master_id}` ルーム）の両方で、登録済みの推しに限定
- **つぶやき削除の権限チェック**：削除は投稿者本人のみ。サーバー側で所有者を検証し、
  他人の削除リクエストは 403 で拒否（フロントの表示制御だけに依存しない）
- **公開範囲を2種類に整理（仕様変更）**：つぶやきの公開範囲を4種類から**「全体」「同じ推し」の2種類**に削減。
  「非公開」は日記帳、「同じイベント参加者」はイベントチャットが役割を担うため廃止。
  `posts.event_id` に依存する処理を撤去し、Socket.ioの配信ルームも `public_all` と `oshi_{oshi_master_id}` の2種類に整理
  （`event_{event_id}` ルーム自体はイベントチャットで引き続き使用するため削除しない）。
  既存データは削除せず移行：「非公開」の投稿は投稿者本人のみ閲覧可のまま、「同じイベント参加者」の投稿は「全体」に変換

### 2026-07-02（第3弾：新機能6件）
- **貯金サポートAI**：イベントの貯金画面からAIに相談できる（進捗に応じた助言・励まし）。
  Anthropic APIの軽量モデル（Haiku系）を使用し、コンテキストには**貯金残高（入金−出金）**・目標額・残り日数を渡す
  （参戦記録の合計は使わない）。1ユーザー1日10回まで。**APIキーはサーバー側の環境変数 `ANTHROPIC_API_KEY` のみで管理**し
  フロントには露出しない。キー未設定でもルールベースの助言にフォールバックして動作
- **ログインのアカウントクイック選択**：この端末で過去にログインしたアカウントをタップで選択（IDだけ自動入力、
  **パスワードは保存・自動入力しない**）。履歴は端末のlocalStorageのみ。「別のアカウントでログイン」導線あり
- **プロフィールに登録推し一覧**：自分・他ユーザーのプロフィールに登録中の推しを表示（非公開ユーザーは推し友以外に非表示）
- **つぶやきから他ユーザーのプロフィールへ**：投稿者のアイコン/名前をタップで `/users/:id` へ遷移し、その場で推し友申請できる
- **ブロック機能**：`blocks` テーブル。ブロックすると申請・DM遮断、つぶやき相互非表示、おすすめ除外、既存の推し友関係は自動解除
  （判定はすべてサーバー側）。プロフィール画面から操作
- **トーク一覧のピン止め**：`pinned_chats` テーブル。ピンしたトークを一覧の最上部にまとめて表示

### 2026-07-02（第2弾修正：着せ替え・貯金の仕様訂正＋全画面ローディング）
- **推しの着せ替え（仕様訂正）**: 承認済み画像が誤って「個人のプロフィールアイコン」になっていたのを修正。
  承認済み画像は**その推しの表示画像**として、`user_oshi_display_image` テーブルでユーザーごとに選択。
  選んだ画像は**そのユーザー自身の画面でのみ**（推しタイル・推し詳細・ホームの推し一覧など）反映され、
  他ユーザーには影響しない（同じ推しにユーザーごと別画像もOK）。未選択なら `oshi_master.image_url`（デフォルト）を表示。
  選択UIは推し詳細ページに設置。プロフィールの個人アイコン（自由アップロード）は従来通り別機能として不変
- **貯金目標（仕様訂正）**: 進捗を参戦記録（支出）合計から算出していたのを廃止し、
  **貯金（入出金）を参戦記録と完全に分離**。`savings_transactions`（deposit/withdrawal）を新設し、
  残高＝入金合計−出金合計で算出。達成度バーのタップで入出金モーダルを開き、貯金・引き出しが可能
  （目標未達でもいつでも引き出せるが、**残高を超える出金は不可**）。入出金履歴も表示。
  ※ 参戦記録のデータ・ロジックには一切変更なし
- **全画面ローディング**: 「取得中／取得完了で0件／データあり」の3状態を明確化。
  取得中は紙トーンのスタンプ演出（`Loading`）を表示し、「データがありません」は**取得完了かつ0件のときだけ**表示。
  推し・予定・記録・グッズ・つぶやき・日記・チャット・イベント・貯金・履歴・アルバム等の全画面に適用
- ※「貯金サポートAI」は本リポジトリには未実装のため該当修正なし。貯金残高の算出は `getSavings()` に一元化済み

### 2026-07-02（第2弾：バグ修正＋新機能）
- **バグ修正**: モバイルで日付詳細ビュー等のモーダル内ボタンがボトムナビ・ホームインジケータと
  重なって押せない問題を修正（モーダル下端に `safe-area-inset` 分の余白を確保）
- **仕様変更（予定共有）**: 「共有する/しない」の二択から、**共有先の推し友を個別に選ぶ方式**へ変更
  （`schedule_shares` テーブル新設。旧 `is_shared=true` の予定はその時点の推し友全員に共有した状態へ自動移行）
- **推しの着せ替え**: ユーザーが推し画像を管理者に申請 → 管理者が承認/却下 → 承認済み画像を
  「着せ替えギャラリー」からアイコンに選択可能（審査・判定は必ずサーバー側）
- **共有アルバム**: DM・イベントチャットのルームごとに共有アルバム。チャットの画像を後から保存する導線あり
- **日記帳**: 個人のオタ活日記帳（ページめくりUI）。公開範囲はつぶやきと**同じサーバー側判定ロジックを再利用**
  （`private`/全体/同じ推し/同じイベント）。つぶやき（SNS的タイムライン）とは別画面
- **推し詳細ページ**: 推しタイルから遷移。代表画像・ジャンル・登録人数・公式/グッズURL（**URLは管理者のみ編集**）
- **イベント履歴**: 参加済みの過去イベント一覧。紐づく参戦記録・日記もまとめて確認
- **貯金目標**: 参加イベントごとに目標額を設定し、紐づけた参戦記録の合計を進捗バーで表示
- **カレンダー改善**: 予定のない月をグレーアウト＋「予定のある月だけ」表示切替。予定の**時間指定**（開始/終了時刻）に対応し、1日ビューを時刻順に表示
- **チャット強化**: 画像・動画・ファイルの添付（種類に応じてプレビュー/ダウンロード表示）＋**既読機能**
  （DMは「既読」、グループは「既読n人」、未読バッジ）
- **アカウント公開/非公開**: 非公開にすると推し友以外にプロフィールが見えず、おすすめからも除外
- DBはすべて `IF NOT EXISTS` 追加＋冪等マイグレーションで、既存データを保持したまま拡張

### 2026-07-02（第1弾修正）

### 2026-07-02（第1弾修正）
- **バグ修正**: 長文（改行なしの連続文字列を含む）が折り返されず画面外にはみ出す問題を修正。
  全画面共通で `overflow-wrap: anywhere` を適用し、つぶやき・チャット・自己紹介など
  自由入力テキストの全表示箇所がコンテナ幅に収まるようにした
- **バグ修正**: アプリを開いて見ている間はOSのプッシュ通知を出さないよう変更。
  Service Workerが表示中のウィンドウを検知した場合は `postMessage` でページへ渡し、
  紙手帳トーンのアプリ内トースト（上からスライドイン・5秒で自動消滅・タップで該当画面へ遷移）で表示。
  アプリを閉じている・バックグラウンドのときは従来どおりOS通知
- **改善**: ホーム画面で参加中の今後の予定・イベントを**すべて上部にまとめて表示**。
  いちばん直近の予定のみワインレッド（#8B3A4A）のカードで強調し、2件目以降は控えめな配色の一覧表示

## ⚠️ プロトタイプとしての割り切り

- 画像はBase64でDBに保存（2MB上限）。本来はストレージサービスを使うべき
- テストコード・CI/CDは未整備（動作優先。ただしAPI・リアルタイムの疎通は手動テスト済み）
- 決済機能・外部SNS連携なし（金額は数値入力、つぶやき・トークはアプリ内完結）
